<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.tech.lfy.mapper.LoginMapper">

    <select id="getLoginUser" resultType="com.tech.lfy.model.UserEntity"  parameterType="map">
        SELECT
          id, user_name as userName, password, invitation_code as invitationCode,
          `type`,`status`,create_time as createTime,parent_id as parentId
        FROM user
        WHERE user_name = #{userName} AND password = #{password}
    </select>

</mapper>